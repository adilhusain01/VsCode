from tkinter import *

root = Tk() #a window  widget

mylabel = Label(root, text = "hello world")  #label that will show info

mylabel.pack() #shoving it into the screen with the amount of info only of size

root.mainloop() #loop keeps showing the widget